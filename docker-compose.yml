services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: backrooms
      POSTGRES_PASSWORD: backrooms
      POSTGRES_DB: backrooms
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
